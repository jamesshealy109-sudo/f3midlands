import React, {useMemo, useState} from 'react';
import aos from '../data/aos.json';
const regions=['All','Lexington','Columbia','Lake Murray','Camden','Saluda'];
const types=['All','Bootcamp','Run','Ruck','Gear','Sports','Mobility'];
export default function AOExplorer(){const[region,setRegion]=useState('All');const[type,setType]=useState('All');const[q,setQ]=useState('');const filtered=useMemo(()=>aos.filter(a=>(region==='All'||a.region===region)&&(type==='All'||a.type===type)&&JSON.stringify(a).toLowerCase().includes(q.toLowerCase())),[region,type,q]);return <section id="aos" className="section"><div className="section-title"><p>Find your AO</p><h2>Search by region, workout type, or town.</h2></div><div className="filters"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search AO, city, address..."/>{regions.map(r=><button className={region===r?'active':''} onClick={()=>setRegion(r)}>{r}</button>)}</div><div className="filters slim">{types.map(t=><button className={type===t?'active':''} onClick={()=>setType(t)}>{t}</button>)}</div><div className="grid aos">{filtered.map(a=><article className="ao-card"><div className="tag">{a.region}</div><h3>{a.name}</h3><p className="type">{a.type}</p><p><b>{a.days}</b> · {a.time}</p><p>{a.address}</p><p className="notes">{a.notes}</p><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(a.address)}`} target="_blank">Directions →</a></article>)}</div></section>}
